{
  "//": "Antigravity hooks (best-effort). XÁC MINH bằng lệnh /hooks trong Antigravity. Nếu không nhận: thử đặt ở .agents/hooks.json hoặc ~/.gemini/config/hooks.json, và chỉnh 'matcher' cho khớp tên tool sửa file của bản bạn.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "apply_patch|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null) && bash \"$ROOT/.claude/hooks/format-dirty.sh\"",
            "timeout": 30
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null) && bash \"$ROOT/.claude/hooks/run-related-tests.sh\"",
            "timeout": 180
          }
        ]
      }
    ]
  }
}
