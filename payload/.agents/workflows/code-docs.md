---
description: Sinh tài liệu code nội bộ cho developer BE vào docs/<Module>/<Feature>.md
---

Đọc `docs/ai/PROJECT-CONVENTIONS.md` rồi `docs/ai/prompts/generate-code-docs.md` và thực thi đúng prompt đó.

Module/class cần viết docs: chỉ định trong `$ARGUMENTS`; nếu chưa nêu rõ thì HỎI lại.

Ghi vào `docs/<Module>/<Feature>.md`. Suy ra behavior từ code + test thật, không đoán từ tên method. KHÁC `api-docs/` (contract cho FE).
